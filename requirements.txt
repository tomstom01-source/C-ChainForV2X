# C-Chain for V2X - Requirements

# Install dependencies with:
# pip install -r requirements.txt

# Core cryptographic library for RSA operations, signatures, and key management
cryptography>=41.0.0

# Note: All other dependencies are part of Python's standard library: